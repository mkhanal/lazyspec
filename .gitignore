# A throwaway consumer repo, rebuilt by sandbox/run.sh on every run.
sandbox/demo/

# Operating systems
.DS_Store
.AppleDouble
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
*~

# Editors
.idea/
.vscode/
*.swp
*.swo

# Node, for the test runner
node_modules/
npm-debug.log*
yarn-error.log*
.npm

# Per-contributor agent state. The committed configuration - .claude/,
# .agents/, .cursor/, AGENTS.md - is deliberate and stays in git; these
# are the local, personal and generated files that sit beside it.
CLAUDE.local.md
.claude/settings.local.json
.claude/*.local.json
.aider*
.opencode/cache/
.codex/log/
.gemini/tmp/
.continue/
sandbox/install-demo/
